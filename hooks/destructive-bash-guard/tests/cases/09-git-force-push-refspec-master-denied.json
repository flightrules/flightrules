{
  "name": "git push -f origin HEAD:master (refspec form) is denied",
  "stdin": {
    "hook_event_name": "PreToolUse",
    "tool_name": "Bash",
    "tool_input": {"command": "git push -f origin HEAD:master"}
  },
  "expect": {
    "exit": 0,
    "stdout_json": {"hookSpecificOutput.permissionDecision": "deny"},
    "stdout_contains": "git-force-push"
  }
}
