{
  "name": "invalid JSON stdin exits 0 silently, no notification fired (fail open)",
  "stdin_raw": "this is not json{{{",
  "env": {
    "FR_NOTIFY_CMD": "printf '%s' {message} > notify.txt"
  },
  "expect": {
    "exit": 0,
    "stdout_empty": true,
    "file_absent": ["notify.txt"]
  }
}
